def calculate_pric(quantity, price):
    """Calculate total price given quantity and unit price."""
    return quantity * price

    def format_currency(value):
        """Format a number as currency."""
        return f"${value:,.2f}"
        